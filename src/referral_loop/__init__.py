"""Referral Loop Closure — deterministic HL7 v2 loop tracking.

Deliberately empty. Any import here lands in the closure of every submodule
and is asserted against by tests/test_referral_import_closure.py.
"""
