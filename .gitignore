__pycache__/
*.py[cod]
.pytest_cache/
.coverage
.coverage.*
htmlcov/
*.egg-info/
build/
dist/
# cli.py defaults its database here. A PHI-bearing SQLite file must never be
# committable by accident.
data/
.venv/

# Nothing of these kinds has ever been committed. Belt and braces, because the
# cost of the first mistake is not recoverable once the repo is public.
*.db
*.sqlite3
*.key
*.pem
.env
